import { css } from "lit";

export const styles = [
  css`
    h1 {
      font-size: 2rem;
    }
    p {
      margin-bottom: 1em;
    }
    a {
      color: var(--accent);
    }
    a:hover {
      color: var(--accent);
    }
    p {
      font-size: 1.2rem;
    }
    ul {
      font-size: 1.1rem;
    }
  `,
];

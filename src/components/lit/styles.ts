import { css } from "lit";

export const styles = [
  css`
    .head-title {
      font-size: 2rem;
    }
    a {
      color: var(--accent);
    }
    a:hover {
      color: var(--accent);
    }
    p {
      font-size: 1.3rem;
      margin: 0 0.2rem;
    }
    ul {
      font-size: 1.2rem;
    }
  `,
];
